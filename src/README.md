# Source

Implementation lands here as phases complete.

```text
src/
├── extension/     # Pi extension (TypeScript) — main integration
│   ├── prompts/   # pi-coding-core, lisptc-channel
│   └── providers/ # capability registry
├── prelude/       # Lisp loaded into REPL (mind API, bootstrap)
└── host/          # Optional shared TS helpers (recall, format)
```

Until phase 0 scaffolding lands, this directory holds structure and placeholders only.
